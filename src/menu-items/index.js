import other from './other';
import pages from './pages';
import adm from './menuItems';
import paginas from './paginas';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [adm, pages, other, paginas]
};

export default menuItems;
